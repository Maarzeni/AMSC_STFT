# System (apt) packages needed to build and run AMSC_STFT natively.
# Not a pip file — see analysis/requirements.txt for the Python-side deps.
#
#   cat system-deps.txt | grep -v '^#' | xargs sudo apt-get install -y
#
# See the README ("Getting Started" → "Requirements") for the compiler
# version and network-access notes this list doesn't cover.

# Compilers and build tools
build-essential
cmake
git

# HPC libraries (OpenMP, MPI)
libomp-dev
openmpi-bin
libopenmpi-dev
