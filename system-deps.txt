# System (apt) packages required to build and run the C++ project.
#
# Consumed by Singularity.def and by the CI workflow as:
#     cat system-deps.txt | grep -v '^#' | xargs apt-get install -y
# so every entry must be a bare Debian/Ubuntu package name, one per line.
#
# NOT a pip list: see analysis/requirements.txt for the Python side.
# Not needed at all when working inside the container, which already has all
# of this baked in.

# Compilers and build tools

build-essential
cmake
git

# HPC libraries (High Performance Computing)

libomp-dev
openmpi-bin
libopenmpi-dev
